spring:
  application:
    name: MoneyManagement

  datasource:
    url: jdbc:mysql://localhost:3306/MoneyManagement?useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Rishabh@28

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

  security:
    user:
      name: admin
      password: Rishabh@123

  mail:
    host: smtp-relay.brevo.com
    port: 587
    username: ${BREVO_LOGIN}
    password: ${BREVO_PASSWORD}
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          from: ${BREVO_MAIL}

server:
  port: 8080



app:
  activation:
    url: ${MONEY_MANAGER_BACKEND_URL}